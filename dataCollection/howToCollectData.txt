make a folder for the trial name and move into it
call demographics.pyy information from that folder

put on device

ssh into both pi's

/////Calibration
run StreamVideo.sh
    top pi add args
        2222 -rot 270
    bottom pi add args
        2223 -vf -hf -rot 90
check to see if horizon is level


have them open and close their hand every 3 seconds and check if LED on arduino nano flashes on and off

/////Capture
run on the bottom pi
    bash handcam/captureBottom.sh TRIAL_NAME 

run on the top pi
    bash handcam/captureTop.sh TRIAL_NAME

if there is an error reapeat the BNO command until it works, it might take a good number of tries (>10)
    python3 handcam/BNOLogging.py TRIAL_NAME 


start live tagging program from the directory with the trial name 

for every exersize the experement type and the object will be pre populated, update if neccissary by pressing e
press enter when prompetd to indecate the start of the experment and again after

after all experements are over exit and kill all of the programs and consolidate the data
    move bottom cam video to computer

    move top cam video, IMU data, and pressure data to computer

    move the data from the pi's to the folder with the trial numbers



later run split files command passing in the location of your trial folder
